/**
 * Fill these in from Supabase → Project Settings → API.
 *
 * The anon key is PUBLIC by design — it ships in every browser that loads the
 * site, and row level security is what actually protects the data. Never put
 * the service_role key here; that one only ever lives in GitHub Actions secrets.
 */
window.ROADWORKS_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-REF.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY'
};
